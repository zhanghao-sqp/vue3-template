declare function formatDate(date: Date, fmt: string): string
declare function parseDate(date: Date, fmt: string): Date
declare function start(zIndex: number, color: string, opacity: number): void
declare function close(): void

export { formatDate, parseDate }
